Automatically connect to a list of IRC networks

This module is required in order for Erebot to work properly.
You should probably always install it.
When loaded in the configuration of a particular server, it
makes Erebot try to automatically connect to that server.
